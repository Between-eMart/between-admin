services:
  between-admin-prod:
    container_name: between-admin-prod
    build:
      context: .
      dockerfile: dev.dockerfile
    command: ["nginx", "-g", "daemon off;"]
    ports:
      - "127.0.0.1:1050:80"
